# Apollo GraphOS Configuration (Optional)
# If you want to publish your schema to Apollo GraphOS, configure these variables
# Otherwise, the router will run in supergraph mode with a local schema file
APOLLO_KEY=service:your-graph-name:your-api-key-here
APOLLO_GRAPH_REF=your-graph-name@current

# Dash0 Configuration
# Get your API token from https://app.dash0.com/settings/api-tokens
# Get your ingestion endpoint from https://app.dash0.com/settings/endpoints

# Dash0 API Token (required for metrics and traces)
DASH0_AUTH_TOKEN=Bearer your-dash0-token-here

# Dash0 Region/Endpoint (update with your region)
# Available regions: us-west-2, us-east-1, eu-central-1, etc.
DASH0_REGION=us-west-2
DASH0_METRICS_ENDPOINT=https://ingress.${DASH0_REGION}.aws.dash0.com/v1/metrics
DASH0_TRACES_ENDPOINT=https://ingress.${DASH0_REGION}.aws.dash0.com/v1/traces

# Or specify full endpoints directly:
# DASH0_METRICS_ENDPOINT=https://ingress.us-west-2.aws.dash0.com/v1/metrics
# DASH0_TRACES_ENDPOINT=https://ingress.us-west-2.aws.dash0.com/v1/traces

# Environment and Service Configuration
ENVIRONMENT=demo
SERVICE_NAME=apollo-router-demo
SERVICE_VERSION=2.7

# Load Testing Configuration
VEGETA_RATE=5
VEGETA_DURATION=0
VEGETA_TIMEOUT=10s
