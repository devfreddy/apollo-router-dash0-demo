{
  "name": "willful-waste-bot",
  "version": "2.0.0",
  "description": "Locust-based load testing for Apollo Router GraphQL API with OpenTelemetry",
  "main": "locustfile.py",
  "scripts": {
    "start": "locust -f locustfile.py --host=http://apollo-router:4000",
    "dev": "locust -f locustfile.py --host=http://localhost:4000"
  }
}
